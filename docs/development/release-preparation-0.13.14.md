# Desktop 0.13.14 / Agent 0.13.4 发布准备

## 范围与审批

- 用户确认从 0.13.13 升至 0.13.14，现有七个测试修改及三个验证辅助文件一起提交。
- 不构建实验性 LoongArch 预览，不运行本地生产构建、打包或安装包启动探针。
- 准备起点为 `ff746bdc1add6a12227ccb61a7794f654aaaa8ba`。
- 最近可达稳定标签为 `v0.13.12`。已公开发布的 `v0.13.13` 指向
  `7756231`，其发布准备提交不在当前 main 祖先链上；两项产品修复已包含，
  0.13.13 的包内发布说明也已保留。需同时核对两个范围，不重复宣传已发布修复，
  不移动标签或重写历史。
- 用户随后确认 Agent 也末位加一，目标为 0.13.4。已批准联合发布的双语说明，
  以及验证通过后一起提交本地候选；最终候选 SHA 仍须另行确认。
- Agent 范围为 `agent-v0.13.3..HEAD`，生产改动来自 `7adae97`。
- 用户批准提交并推送两个远端、等待 CI；标签仍待精确提交确认。
- Fetch 确认两个远端 main 均为 `7756231b3bdfa148f1b2702e4ff3fc3bd0cb057e`。
  用户批准以普通合并保留双方历史，保留本次版本和已批准说明，
  补回历史发布文档；不强推、不改动已有标签。
- 本地候选提交为 `d23af99`。普通合并解决后，仅新增或修改文档；
  产品源码、测试、依赖、版本文件及两份已批准说明与该候选完全一致。
  合并后 `v0.13.13` 恢复为可达发布基线，先前祖先链差异仅记录准备起点状态。

## 交付与数据边界

- 本轮联合准备 Desktop 0.13.14 和独立 Agent 0.13.4，不覆盖历史 Agent 包。
- 桌面包含远程结果未知后显式发送新消息的恢复修复；Agent 完成确认、
  空闲回收失败处理和旧安装清理随 0.13.4 交付。
- Agent 发布流程读取本次 package.json 作为最低 Desktop 版本，因此需先升级
  Desktop 至 0.13.14，再下载并更新 Host。Node、OpenCode、Continue 锁定版本不变。
- 两个候选分别使用 `v0.13.14`、`agent-v0.13.4`，Agent 不替代 Desktop Latest。
- 数据库从 schema 46 升至 47。升级前需完整备份用户数据，包括数据库和
  相邻 notes/ 目录；旧客户端不能打开升级后的数据库。
- 已检查中英文功能矩阵，更新当前版本、监督回顾、待办能力及独立 Agent
  交付边界。其他平台和功能的既有未验收事项继续保留。

## 验证

- 首次 `npm test`：5,125 项通过、74 项跳过、2 项失败。失败均为版本已更新而
  说明尚未获批写入导致的当前版本缺失，未修改断言规避。
- 联合说明写入后的 `npm test`：435 个文件通过、10 个文件跳过；
  **5,127 项通过、74 项跳过，0 失败**，耗时 685.49 秒。
- `npm run typecheck`、`npm run lint`：首轮与联合候选复验均通过。
- `npm run release:notes:verify`：联合说明写入后通过；Desktop 三处版本一致，
  当前说明仅一项，Agent lock 与独立双语标题一致。
- 合并后复验：发布说明校验、9 项发布说明测试、完整 typecheck、lint、
  `git diff --check` 通过。因合并仅恢复文档，未重复运行完整测试和 Host 探针。
- 当前源码 Linux x64 完成/历史恢复探针通过：两轮 Ask 均完成，Runtime 重建后
  保留会话历史，每轮结束及最终退出时所属进程数均为 0。
- 隔离旧安装清理探针通过：真实 CLI 回收所属孤儿进程、删除对应旧 payload，
  保留存活旧 daemon、活动工作、历史及 current/candidate，重复清理无重复删除。
- 本轮新增真实模型调用 **2 次**，模拟模型调用 0 次；清理探针模型调用 0 次。
- 两个探针从当前源码重新生成临时测试脚本，仅操作共享 Linux x64 Host 的独立
  `/root/tmp/gb-lifecycle-*` 与 `/root/tmp/gb-upgrade-*` 测试目录，不替换共享安装。
  探针沿用历史测试身份版本，不是正式签名 0.13.4 包安装验收，也不代替
  Linux arm64、macOS arm64 原生 CI 或完整 Host 更新界面验证。
- 复现命令沿用[远程 Host 技术设计](../features/remote-host/technical-design.md)
  中 lifecycle-host 和 upgrade-cleanup 脚本；本轮生命周期参数为
  `driver --completion-only`，使用真实模型而非 `--fake-model`。
- 候选主分支 CI、生产构建、六平台原生打包及公开发布验证：尚未执行。
