# Desktop 0.13.16 发布准备

日期：2026-09-26。当前为候选，不代表已发布。

## 范围与批准

- 用户确认 Desktop 0.13.16、Agent 保持 0.13.4、不构建 LoongArch 预览。
- 基线为 `325b702934ba0f0c076f27e926a897267262ab55`，用户明确批准纳入新提交的
  Supervisor 界面修改。相对已发布 `v0.13.15` 包含：
  - `e6ac073`：监督回顾持续分批处理，保留主动暂停、单次模型超时和并发。
  - `4c7193e`：结构升级不自动回收正常空闲页，准确显示操作状态及保留同启动重试。
  - `325b702`：监督回顾空态、运行状态与活动阶段布局修复。
- 用户已批准中英文三项问题修复及两项升级提示，写入 `resources/release-notes.json`；
  已发布版本说明保留。最终候选 SHA 需另行确认，主分支 CI 通过后才创建新标签。
- `FEATURES.md` 和 `FEATURES.zh-CN.md` 已核对并更新当前版本、持续回顾、
  结构升级及界面行为，删除过时的执行预算暂停描述。

## 数据与 Agent 边界

相对 0.13.15 不提高 schema，仍为 47。不新增持久化迁移标记，不改写用户数据库。
旧格式仍执行必要转换；普通结构升级不因空闲页执行全库 VACUUM。
监督回顾仍只读、无工具，模型调用可能收费；取消固定时长暂停后可连续处理更多批次。

本轮没有 Agent、Runtime、模型桥、部署锁或远程协议修改；本机与远程历史共用桌面升级
实现。监督通过桌面 Ask 编排，不改变 Host 运行路径，不要求 Agent 更新或新 Host 部署。
本轮实际模型请求数为 0。

## 验证

在独立工作树准备，避免其他工作修改影响候选，复用相同锁定依赖，未复制本机私有环境文件。
候选第一轮全量发现 `HeartbeatCenter.test.tsx` 的两种语言仍要求已移除的重复说明存在；
改为断言说明不存在，保留范围、操作、标题及页面内容校验，专项 **42 项通过**。

最终候选完整测试：**5,128 通过、84 跳过、1 失败**。唯一失败为未修改的
`mermaid-viewer.electron.test.ts` 子进程退出码 null；首次单独复跑被工具的 150 秒
上限中断，随后按用例原有时间窗口复跑 **1 项通过**，覆盖九种显示场景和十次实际 PNG
导出。未修改该用例、提高其自身超时或弱化断言，独立复跑不替代全量结果。
`npm run typecheck`、`npm run lint`、`npm run release:notes:verify` 均通过。

先前迁移定向 47 项通过；最终开发期全量为 5,137 通过、74 跳过、两项 Electron
失败，失败用例独立复跑两项通过。该记录不是本候选的全量或 CI 通过证明。

未运行本地生产构建、打包或安装包启动。主分支 CI、六目标原生打包和公开发布核验待完成。
