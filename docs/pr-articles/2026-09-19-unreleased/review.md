# 综合更新稿核查记录

## 状态与范围

- 状态：**草稿，未发布，截图待补**。
- 首次建档及检查日期：2026-09-19。
- 按用户要求将原魔法笔记重点稿与常规更新稿合并，只保留一份[正文](./article.md)及本记录，原主题子目录稿件移除。
- 目标 tag：桌面 `v0.13.7`，Agent `agent-v0.13.1`；截图和外部发布仍待完成。
- 目标提交：发布资料候选提交待最终提交后填写；当前已推送基线为 `9836a4cc9d82144496250a0e3154316b4dd0d9c4`。
- 基线：正式桌面 tag `v0.13.6`，对应 `a383b2d11f1e834a30b08b0786ca0398da849d95`；当前比较区间为 `v0.13.6..HEAD`，39 个提交。
- 当前 HEAD 不在正式 tag 上；最近可达 Agent tag 为 `agent-v0.13.0`，Agent 比较区间为 `agent-v0.13.0..HEAD`，不代表新版 Agent 包已发布。
- 最初核查时工作区干净；合并前只有本轮两篇文章、核查记录与索引改动。未修改产品代码，未创建提交、推送、tag 或外部发布。

## 选题与结构

类型：按用户任务区分重点的综合 PR，沿用[常规模板](../README.md#常规与综合稿模板)组织不同功能，画布笔记与文档处理按[重点模板](../README.md#重点稿模板)展开入口、操作和结果。
按用户最终确认，正文固定为“新增功能 → 功能改进 → 问题修复”。重点排序只作用于各段内部，
问题修复在文末逐项列出，升级说明移到笔记功能旁。此前混写的“日常使用的调整”已拆分。

推荐标题：**GoodBuddy 更新预览：画布笔记、文档处理与应用管理**。

目标读者是使用 GoodBuddy 记录内容、处理资料和运行 Agent 的桌面用户。判断依据是[产品定位](../../../README.zh-CN.md)和本轮对实际任务的改变，不按代码量、技术名称或用户最近提及顺序排名。没有使用频率数据，不宣称哪类用户最多或收益最高。

| 层级 | 用户可以完成的任务 | 篇幅及边界 |
| --- | --- | --- |
| 核心重点：画布笔记 | 在分页纸张里混排文字、笔迹和图片，批注 PDF，保存后继续编辑或导出 | 完整介绍新记录方式；PDF 批注只是其中一部分。书写和保存不要求模型或 OCR 服务，因此用它开篇可直接展示工作成果，不从配置表单讲起 |
| 核心重点：文档处理 | 核对解析图文、选图提问、重启回看、重新解析；有团队 OCR 服务时接入使用 | 按资料处理流程合并预览、附件与 OCR；HTTP PaddleOCR-VL 充分展开，但不假定每位读者都先部署服务 |
| 产品方向：应用管理 | 当前管理内置应用，后续从组织私有市场获取应用 | 用户明确应用管理是未来应用市场入口，并保留私有化部署；与既有 FR-15 一致。单独成节，当前能力和规划明确分开，不降为排序按钮修复，也不宣称市场已上线 |
| Agent 用户新增能力 | 在对话里观察清单；远程项目选择 Continue | 清单与 Runtime 选择分别说明；远程功能保留主机及组件前提 |
| 简述新增：会话置顶 | 常用会话固定在前，重启保留 | 属于用户以前没有的能力，在新增功能末尾简述 |
| 功能改进 | 连续浏览笔记、常用操作布局与反馈 | 单独放入第二段，不混入故障修复 |
| 问题修复 | 历史截断、旧会话查找、取消后续发、浮层遮挡、网页链接、图谱预算 | 第三段逐项说明问题与改后行为，不因位于文末而省略 |

文档处理和画布笔记是独立入口，不暗示自动摘录、一键从附件生成笔记或跨应用自动联动。应用市场私有化是后续方向，不将此词扩展为本轮已提供服务端或任何依赖都可离线安装。

富文本笔记、待办、AI 评论和本地 OCR 原已存在，不写成本轮从零新增。HTTP 服务接入、画布和远程 Continue 是本轮新增能力。正文不宣称演示已经执行，不编造分析结果、效率数字或客户案例。

## 事实与来源

下表根据范围内 diff、最终实现及功能记录核查。提交信息用于定位，不作为唯一依据。

| 正文事实 | 提交线索 | 实现与权威说明 | 验证边界 |
| --- | --- | --- | --- |
| 分页画布、画笔、高亮、正文、图片、PDF 底版及导出 | `17186b0` | [画布 core](../../../src/renderer/src/magic-canvas/canvas-note.mjs)、[集成说明](../../../src/renderer/src/magic-canvas/README.md) | 已有 Electron core 和生产 IPC/存储记录；本轮没有重新运行 |
| 连续记录、缩略图索引、手动保存、新建后清空输入区及离开保护 | `17186b0`、`935a8f5`、`c98eb69` | [Workspace](../../../src/renderer/src/MagicNotesWorkspace.tsx)、[工作区设计](../../features/magic-notes/technical-design.md#continuous-record-workspace) | 功能级 Electron、真实数据库/文件及组件证据各自独立，不合并宣称完整 App 验收 |
| 25%–300% 缩放、适应宽度，不改变导出尺寸 | `c98eb69` | [缩放定义](../../../src/renderer/src/magic-canvas/README.md#view-zoom) | 历史记录为 41 项 Electron core 检查通过；未验证物理设备或 macOS |
| 图片 20 MiB、PDF 32 MiB、最多 50 页 | `17186b0`、`c98eb69` | [编辑限制](../../../src/renderer/src/magic-canvas/README.md#editor-limits) | 20 MiB 文件边界有 Electron 和真实文件存储测试；不能推导超大解码图片性能 |
| 按设置发送前 1～8 页（默认 1）、文字回退、手动或保存后分析 | `17186b0` 及当前工作树页数设置 | [分析器](../../../src/main/magic-notes/magic-note-analyzer.ts)、[分析契约](../../features/magic-notes/technical-design.md#canvas-analysis-and-comments) | 页数设置已通过真实 Electron 设置 UI、生产 IPC 和本地替代模型验证，覆盖显式及自动换页；真实模型调用 0 次 |
| 正文迁移到本地文件，备份需数据库和 notes | `17186b0` | [存储实现](../../../src/main/magic-notes/magic-note-storage.ts)、[备份契约](../../features/magic-notes/technical-design.md#backup-and-restore) | 有构造旧库迁移及重开证据，不是用户真实历史库迁移；没有一键备份或跨设备同步声明 |
| 附件保存、重启回看、文档选图、图片 OCR、队列恢复 | `935a8f5`、`a9b59ce`、`6f06fce` | [文档处理实现](../../features/document-processing/implementation.md)、[App](../../../src/renderer/src/App.tsx)、[解析服务](../../../src/main/document-parsing-service.ts) | 保存的是被草稿、消息等引用的资产，不是永不删除；旧历史缺失文件不自动补全 |
| HTTP PaddleOCR-VL 服务接入、解析模式、认证、图文预览、参数与文件测试 | 同上 | [数据流](../../features/document-processing/implementation.md#解析与设置)、[验证](../../features/document-processing/progress.md#2026-09-19-实现与验证)、[HTTP 设置](../../../src/renderer/src/HttpOcrSettings.tsx)、[解析设置](../../../src/renderer/src/DocumentParsingSettingsSection.tsx) | HTTP 路径不要求本地权重；快速路径不上传，显式图片 OCR 独立调用；跨页合表仅有连续两页样例证据，其他高级项只描述可配置，不保证效果 |
| 顶部执行清单、受管远程 Continue | `c7a9380` | [只读清单组件](../../../src/renderer/src/RuntimeChecklistStrip.tsx)、[清单设计](../../features/assistant-workbar/runtime-checklist-technical-design.md)、[远程记录](../../features/remote-host/runtime-checklist-validation.md) | 仅原生 OC/CN 清单；已有 Windows 桌面到共享 Linux x64 Host 记录，不代表所有平台或 Runtime 实测；正式包待确认 |
| 应用管理、共享排序、启用与常驻 | `bbae893`、`ca3c919`、`5aad0cf` | [ApplicationCenter](../../../src/renderer/src/ApplicationCenter.tsx)、[UI 定义](../../features/application-tool-navigation/ui-design.md)、[进度](../../features/application-tool-navigation/progress.md) | 知识库、智能心跳不可关闭；本机推理监控默认不常驻针对新配置或缺省配置，保留已有明确值 |
| 本机推理监控及后续私有化应用市场 | 当前功能见上行；市场方向由用户本轮确认及既有 PRD 定义 | [本机推理 UI](../../features/application-tool-navigation/ui-design.md#91-本机推理管理)、[FR-15](../../features/application-tool-navigation/prd.md#fr-15-私有化市场与-yaml-交换后续) | 监控只承载已支持的本地服务操作，不声称所有引擎有独立指标；私有市场是规划，当前没有目录获取／安装功能，不改变其他下载源 |
| 会话置顶、列表和搜索范围、发送历史 | `625182c`、`375b215` | [数据库](../../../src/main/assistant/assistant-database.ts)、[App](../../../src/renderer/src/App.tsx)、[历史存储](../../features/assistant-workbar/execution-history-storage.md) | 置顶持久化；移除数量截断不等于无限模型上下文，也不恢复已丢失历史 |
| 浏览器弹出链接、浮层菜单及通知、图片下载按钮 | `f1d7197`、`ec08109`、`b5d8204` | [浏览器设计](../../features/assistant-workbar/browser-tabs-technical-design.md)、[浮层验证](../../features/document-processing/progress.md#2026-09-19-全局浮层复验)及对应源码 diff | 仅描述交互变化，不增加“所有窗口永不遮挡”等保证 |
| OpenCode 取消后续发、已完成回复清除停止状态 | `94852ac` | [真实连续请求记录](../../features/assistant-workbar/consecutive-requests-live-2026-09-17.json)、提交中的 Runtime 与 App 差异 | 历史完整 App 验证为 Windows、本地 OpenCode、Ask，保留请求计数缺口；本轮不重跑、不推及全部 Runtime |
| 图谱抽取固定预算与顶部重复状态 | `b5d8204`、`df89d96` | [图谱请求实现](../../../src/main/knowledge/model-extractor.ts)、对应 App 差异 | 图谱保留 Provider 上限；顶部仅取消重复展示，输入区及失败通知保留 |

笔记历史验证见[功能进度](../../features/magic-notes/progress.md)。其中已被替代的单记录布局、SQLite 内嵌正文和旧按钮入口不作为现状。

## 未扩写的内容与效果边界

- 后台会话完成提示只在当前客户端运行期间保留，不能宣传成跨重启通知；最新 HEAD 有相关 App 测试失败记录，暂不作为卖点。
- 图谱取消额外 8192-token 预算已列入末尾问题修复，不写成“输出无限”：OpenAI 交由模型服务，Anthropic 使用连接配置。
- 子代理渲染、队列闪动和工具错误恢复等维护修复不展开为独立能力，不凭提交描述推导性能数字。
- 右侧资源监控和 story-graph 的规划／Demo 不算已交付产品。
- OCR 跨页表格只有特定有效样例，不宣传能恢复任意跨页表格。方向分类、去扭曲、图表、印章、标题层级调整只介绍配置入口，并说明需用实际部署确认效果。HTTP 设置里“尚未验证合并效果”的提示落后于进度记录中的连续两页样例，不据此否认已有证据，也不扩展为普遍成功；本次不修改该源码文案。
- Continue 与 DeepSeek Harness 的本轮真实图片消费未执行，不借附件流程宣传所有 Runtime 均已验证。

## 截图检查与待补素材

已读取[截图步骤](../README.md#截图执行步骤)和[Electron 自动化规范](../../quality/electron-ui-automation.md)，检查结果如下：

- Windows x64，Node `v24.19.0`、Electron `43.2.0`；仓库存在 Main、Preload、Renderer 产物。
- `out` 目录时间约为 2026-09-19 21:19–21:20，目标提交时间为 21:34:42（+08:00）。时间不能证明构建是否包含当时未提交源码，未取得可核对的目标构建身份。
- `out` 标识搜索遇到两个尾点 LICENSE 路径读取错误，未将其视为完整扫描。已有独立 Electron supervisor、完整 App 驱动和视觉探针可参考，但存量图片不作为本轮素材。
- 用户明确选择“先完成文章，截图缺口记入待补清单”。未构建、未启动演示应用、未创建 profile，也未用原型替代截图。

**实际截图 0 张。** 不创建空 `assets/`，正文使用醒目的「待补图」文字标注位置，计划文件名仅作代码文本，不插入不存在的图片链接。取得并审阅图片后替换标注为相对路径图片及真实图注。

后续统一采用中文、浅色主题、100% 应用缩放、1280×800 内容区，画布缩放另记。拟用虚构的两页《示例培训安排》，包含原生文字与插图；不用私人会话或客户文件。

| 优先级 | 建议文件名 | 正常 UI 操作 | 要证明的结果／建议图注 |
| --- | --- | --- | --- |
| 必补 | `assets/01-canvas-note.png` | 新建画布，在纸张上键入正文、添加笔迹及虚构图片，保存后重开 | “同一页画布包含文字、手写标记和图片，保存后可以继续编辑。” 主图展示画布整体能力，不仅展示 PDF |
| 建议 | `assets/02-pdf-annotation.png` | 在另一条画布中导入 PDF，圈画并补文字，保存和导出 | “在 PDF 原页面添加批注。” 导出文件另拍时注明图像合成、不含可搜索文字层 |
| 必补 | `assets/03-document-to-chat.png` | 聊天正常导入虚构扫描 PDF，经真实 HTTP OCR 后打开图文预览，选图加入草稿，再重开核对结果 | “查看解析图文，选择插图加入会话。” 不用测试预览替代正式导入，不显示服务地址或密钥 |
| 可选 | `assets/04-http-ocr-test.png` | 从隔离设置执行真实文件测试，查看结果 | “用实际文件检查服务解析结果。” 配置和测试作为辅助图，不替代用户成果主图 |
| 建议 | `assets/05-app-management.png` | 从应用中心打开管理，调整顺序并重新打开核对 | “在应用管理中打开和排列内置应用。” 不放未来市场原型，不伪造安装目录 |
| 建议 | `assets/06-runtime-checklist.png` | 从远程 CN 项目的正常输入区提交最短清单任务，等待原生更新，展开顶部清单 | “远程 Continue 的执行清单显示在对话顶部。” 不注入假事件，不展示主机地址 |

取得匹配产物后隔离 userData/sessionData/日志，由生产 Main 加载真实 Preload/Renderer。保存后重开核对，等待字体和布局稳定再截图，逐张审阅文字、裁切、状态与隐私。记录构建身份、实际尺寸、输入方式和结果；保留原图，裁切另存，采用图片以相对路径插入正文。

文档主图如声明 HTTP OCR，必须来自真实解析，不用原生文本预览冒充服务结果。后续拍摄前确认可用服务和 OCR 调用授权，真实文本模型的持续授权不自动扩大为收费 OCR 授权；使用最小虚构样例并记录请求数。附件选图复用正式导入取得的结果。远程清单图遵循已有 Host 身份、专用目录及最小模型请求规则，逐次统计 dispatch。本轮仍按用户要求不拍摄、不请求服务。

AI 评论不列为必拍主图。若要展示生成效果，先解决默认模型真实路径验证，不能以 fixture 评论冒充生成结果。

### 正文中的替换与核对标注

正文已在对应位置加入四项「待替换」及一项「待核对」，不只把待办留在此记录中：

| 标注 | 待处理内容 | 完成条件 |
| --- | --- | --- |
| R01 | 标题的预览状态、版本号、发布日期、下载和完整更新日志链接 | 正式版本可下载且链接核验后替换；未发布时保留预览说明 |
| R02 | 画布说明的仓库相对链接 | 外发平台使用对应发布版本的公开完整链接；仓库内阅读可保留相对链接 |
| C01 | 画布分析默认页数及 1～8 页配置 | 按最终发布提交核对，行为变化才替换文字；不以旧基线否定并行更新 |
| R03 | 备份说明的仓库相对链接 | 外发时换为可定位备份章节的公开链接，数据库和笔记目录的备份提示保留 |
| R04 | 远程 Continue 的桌面／Agent／Runtime 版本及获取方式 | 核对正式配套版本、可获取包和安装说明，不用开发测试包替代 |

未来应用市场的规划说明不是等待改成“已上线”的占位，应按真实交付状态保留。普通演示内容、服务地址和密钥不作为正文待填字段，不要求填入组织真实资料。

图片编号按正文出现顺序统一为 01～06，其中 01、03 必补，02、05、06 建议，04 可选。图 01 可同时说明记录索引与连续浏览，不为同一界面再补一张重复图；会话置顶、通用按钮调整和六项问题修复采用文字说明，不强制配图。

发布前逐项处理所有标注：必补图片拍摄后替换，建议／可选图决定不用时删除对应标注；确认图注与实图一致，删除编辑标注说明及已处理的提示。R01 未满足时不将草稿改成正式发布稿。

## 验证与发布前检查

### 已有记录，不是本轮重跑

- 画布、持久化、缩放和布局有定向测试及 Electron 记录，部分使用替代 IPC 或模型输出。
- 文档处理含完整 App、真实服务及部分生产 IPC 场景，其全量通过结论仅对应当时快照。
- 远程 Continue 有开发联合包安装、实际输入框、清单保存、重启、取消后续发记录，不等于正式包可下载。
- 应用导航部分是组件与真实设置存储组合，部分截图尝试失败，不能概称全部 UI 通过。
- 最新 `375b215` 记录为 7 项定向测试、typecheck/lint 通过；全量测试超时前报告 App 完成提示和 WorkspacePrimitives forced-color 断言失败。不能用早期全量通过覆盖当前结论。

### 本轮工作

- 初稿已核查 Git 区间、对应代码、现行说明与验证边界；本次合并未增加功能声明，保留原事实来源与待办。
- 原两稿中文扫描均无阻断项，原 5 个 Markdown 文件的 48 个本地链接及锚点通过。该结果是合并前记录，不代替合并后检查。
- 合并后运行 `deai_scan.py --fail-on-block`：阻断项 0，复核项 3；人工复核保留未发布、PDF 文字层和清单只读／终态说明，均为必要事实边界。
- 合并后 3 个 Markdown 文件、40 个本地链接及锚点检查通过，尾随空格和 UTF-8 替换字符检查通过；`git diff --check` 通过，仅有换行转换提示。文档中无旧主题路径引用，最终改动仅含合并稿、核查记录和索引。
- 仅 Markdown 改动，未运行产品测试、typecheck、lint、构建或 Electron；模型、OCR、知识库调用均为 0，远端 Host 操作为 0。
- 未复制用户配置或启动演示应用，无本轮 profile、凭据副本或监听端口需要清理。

### deai 与功能重点调整（前一轮）

- 用户指出新增功能篇幅不足，本轮将 HTTP PaddleOCR-VL 从附件小节中的一段改为首要功能，补齐无需本地权重、共享配置与独立场景、PDF/PPTX/图片处理、图文预览、过滤及跨页整理、文件测试。远程 Continue 也改为明确的功能介绍，界面调整压缩到末尾。
- 重新核对生产实现、HTTP 设置组件、解析设置组件和远程验证记录。高级选项区分“有配置入口”和“已验证效果”，连接检查不写成解析成功。
- 按仓库 deai-writing 指引逐段润色，将“原生图片输入路径”等实现措辞改为具体操作，删去泛泛的体验描述与重复引导。复扫首次命中一句“不只是一段纯文本”的对照模板，已删去该句，直接介绍图文结果。
- 本轮仅修改宣传目录中的正文、记录及索引。润色期间工作区出现 Agent、画布、文档解析等其他未提交源码和功能文档变更，均保留，不纳入本文固定提交区间，也不修改或宣称已验证。
- 最终 `deai_scan.py --fail-on-block` 复扫：阻断项 0，复核项 2；人工复核保留未发布状态与执行清单只读／终态说明。3 个 Markdown 文件的 42 个本地链接及锚点、标题一致性、尾随空格和 UTF-8 替换字符检查通过，宣传目录 `git diff --check` 通过。
- 截图仍待补，未新增构建、模型或 OCR 请求；产品测试、typecheck 和 lint 未运行，本轮只改宣传文档。

### 用户视角重排与应用方向

- 前一轮按技术名称置顶 HTTP OCR 的结构已被本次双重点结构替代；选题主次以本文“选题与结构”为准。标题明确保留“画布笔记”，不再用 PDF 批注概括其全部能力。
- 核查产品定位、应用管理的 FR-15 及现行本机推理功能。应用市场私有化来自已有规划和用户明确指引，仅宣传为后续方向；正文保留当前四应用、实际服务操作与未来市场的区分。
- 已重新读取外部修改后的远程验证文档。其新增会话 MCP 回归属于其他未提交工作，不扩入本文固定提交范围，也不把替代图片接收端验证说成真实图片生成。
- 原文事实、升级边界及服务费用继续保留；截图计划改为画布混排、文档选图等用户操作结果优先，市场原型不作为已交付截图。
- 本次最终 deai 复扫为 0 个阻断项、3 个复核项；人工复核后保留开发版状态、市场未上线和清单只读／终态限定。3 个 Markdown 文件的 45 个本地链接及锚点、标题一致性、尾随空格和 UTF-8 替换字符检查通过，宣传目录 `git diff --check` 通过，仅有换行提示。
- 本轮仅修改文章、内部记录及索引，其他工作区变更保持不动；未运行产品测试、typecheck、lint 或构建，模型与 OCR 请求均为 0，未拍摄或对外发布。

### 固定三段与问题修复

- 用户确认顺序固定为新增功能、功能改进、问题修复，组内再按用户价值排序。已同步正文和两类模板，修复在文末；会话置顶归入新增功能，连续浏览与操作布局归入功能改进。
- 末尾列出六类具体问题：500 条历史截断、100 个会话范围、OpenCode 取消后续发和停止状态、浮层遮挡、新窗口链接、图谱额外输出预算。新增条目核对已提交差异及历史连续请求证据，不把历史测试说成本轮执行。
- 编辑期间正文的画布分析页数说明及本记录中的相应证据被并行更新，本轮保留。该项已在来源表单列为当前工作树变化，不归入 `v0.13.6..375b215` 的已提交事实；三段整理没有独立重跑其产品验证，发布前应更新目标提交。
- 文案复扫 0 个阻断项、3 个复核项，保留预览状态、市场规划和清单只读边界。6 份文档的 59 个本地链接及锚点、正文及两模板的固定三段顺序检查通过；宣传目录差异和空白检查通过。
- 本次只调整宣传文档，未运行产品测试、typecheck、lint 或构建，未拍摄或调用模型／OCR。

### 发布前仍需

- [ ] 确认最终桌面 tag、提交及可下载产物，更新目录、索引和预览状态，核验下载与完整更新日志链接。
- [ ] 确认远程 Continue 所需正式 Agent／Runtime 包及版本组合，分别记录桌面和 Agent 发布状态。
- [ ] 在最终版本补画布混排与文档选图主图，按需补 PDF 批注、应用管理和远程清单图，复核操作与界面一致；不以市场原型冒充现有功能。
- [ ] 获取最终候选验证结论，保留失败与未覆盖项，不宣称“全量通过”。
- [ ] 保留或扩大 AI 效果宣传前完成真实默认模型路径验证，不增加未经验证的效果承诺或结果截图。
- [ ] 保留备份、手动保存、导出文字层、模型发送及旧附件不补全说明。
- [ ] 仅在用户明确授权后发布正文及采用图片，不附此内部记录。
