"""匿名项目词典模板。

复制为有意义的模块名后，用 ``--rules <模块名>`` 加载。项目规则只收录特定
站点、文档类型或团队确认过的表达，跨项目通用规则应放回 ai_smell_dict.py。
"""

AI_SMELL = {
    "项目禁用表达": [
        r"示例阻断词",
    ],
}

REVIEW_ONLY = {
    "项目复核表达": [
        r"示例复核词",
    ],
}

REVIEW_LOG = [
    {
        "date": "YYYY-MM-DD",
        "rejected": "原句",
        "issue": "评审意见",
        "fix": "最终改法",
    },
]
