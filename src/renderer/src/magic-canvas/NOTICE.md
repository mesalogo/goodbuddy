# PeopleLib Canvas Core

`canvas-note.mjs` and `canvas-flow.mjs` are adapted from PeopleLib 2.1.7,
whose package declares the MIT license and author `peoplelib`.
The source repository did not contain a project-level license text.
The original implementation is retained with GoodBuddy module imports,
document-owned assets, lifecycle fixes, and React integration.

Fabric 7.4.0 and jsPDF 4.2.1 are distributed under MIT; Quill is BSD-3-Clause;
PDF.js is Apache-2.0. Their original license files remain in their npm packages.
The application packaging configuration includes the Fabric, jsPDF and Quill
license files and this attribution notice.
